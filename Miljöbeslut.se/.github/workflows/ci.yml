name: CI

on:
  pull_request:
  push:
    branches: [main, master]

# ─── Shared configuration ───────────────────────────────────────────────────

# Environment variables that do not require a live database (used in jobs
# that only do static analysis / build).
env:
  NODE_VERSION: '22'

# ─── Jobs ────────────────────────────────────────────────────────────────────

jobs:
  # ── 1. Static analysis – runs without a database ─────────────────────────
  typecheck:
    name: Typecheck
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - run: npm run ci:arch:guard
      - run: npm run typecheck

  lint:
    name: Lint
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - run: npm run ci:arch:guard
      - run: npm run lint

  format:
    name: Format check
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - run: npm run format:check

  # ── 2a. Component tests – no database needed ─────────────────────────────
  test-component:
    name: Component tests
    runs-on: ubuntu-latest
    needs: [typecheck]
    permissions:
      contents: read
    env:
      NODE_ENV: test
      JWT_ACCESS_SECRET: ci-access-secret
      JWT_REFRESH_SECRET: ci-refresh-secret
      ADMIN_CONSOLE_USERNAME: admin
      ADMIN_CONSOLE_PASSWORD: admin
      ADMIN_ORG_NAME: Miljobeslut Test Org
      ADMIN_ORG_NUMBER: 999999-0001
      LANTMATERIET_OPEN_MODE: 'true'
      LANTMATERIET_BASE_URL: https://example.invalid
      SLU_API_BASE_URL: https://example.invalid
      SLU_API_KEY: ci-slu-key
      SEARCH_WORKER_ENABLED: 'false'
      DATABASE_URL: postgresql://miljobeslut:password@localhost:5432/miljobeslut_test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - run: cp .env.test.example .env.test
      - name: Component tests
        run: npm run test:component

  # ── 2b. Unit tests – no database needed ──────────────────────────────────
  test-unit:
    name: Unit tests
    runs-on: ubuntu-latest
    needs: [typecheck]
    permissions:
      contents: read
    env:
      NODE_ENV: test
      JWT_ACCESS_SECRET: ci-access-secret
      JWT_REFRESH_SECRET: ci-refresh-secret
      ADMIN_CONSOLE_USERNAME: admin
      ADMIN_CONSOLE_PASSWORD: admin
      ADMIN_ORG_NAME: Miljobeslut Test Org
      ADMIN_ORG_NUMBER: 999999-0001
      LANTMATERIET_OPEN_MODE: 'true'
      LANTMATERIET_BASE_URL: https://example.invalid
      SLU_API_BASE_URL: https://example.invalid
      SLU_API_KEY: ci-slu-key
      SEARCH_WORKER_ENABLED: 'false'
      DATABASE_URL: postgresql://miljobeslut:password@localhost:5432/miljobeslut_test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - run: cp .env.test.example .env.test
      - name: Readiness gate (pilot-preflight)
        run: npm run smoke:pilot-preflight
      - name: Unit tests + coverage (thresholds in vitest.config.ts)
        run: npm run test:unit:coverage
      - name: Upload coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage
          if-no-files-found: ignore

  # ── 3. Integration tests – requires PostgreSQL + PostGIS + pgvector ───────
  test-integration:
    name: Integration tests
    runs-on: ubuntu-latest
    needs: [typecheck]
    permissions:
      contents: read
    services:
      postgres:
        # Custom image providing PostgreSQL 16 with both PostGIS 3.4 and
        # pgvector so that all extensions used by the project are available.
        # See docker/postgres-test/Dockerfile for the build definition.
        image: ghcr.io/jbmbab/milj-beslut-postgres-test:16
        env:
          POSTGRES_DB: miljobeslut_test
          POSTGRES_USER: miljobeslut
          POSTGRES_PASSWORD: password
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U miljobeslut -d miljobeslut_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 15
    env:
      NODE_ENV: test
      DATABASE_URL: postgresql://miljobeslut:password@localhost:5432/miljobeslut_test
      JWT_ACCESS_SECRET: ci-access-secret
      JWT_REFRESH_SECRET: ci-refresh-secret
      ADMIN_CONSOLE_USERNAME: admin
      ADMIN_CONSOLE_PASSWORD: admin
      ADMIN_ORG_NAME: Miljobeslut Test Org
      ADMIN_ORG_NUMBER: 999999-0001
      LANTMATERIET_OPEN_MODE: 'true'
      LANTMATERIET_BASE_URL: https://example.invalid
      SLU_API_BASE_URL: https://example.invalid
      SLU_API_KEY: ci-slu-key
      SEARCH_WORKER_ENABLED: 'false'
      DATABASE_INTEGRATION: 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - run: cp .env.test.example .env.test
      - name: Enable PostGIS + pgvector extensions
        run: |
          PGPASSWORD=password psql -h localhost -U miljobeslut -d miljobeslut_test \
            -c "CREATE EXTENSION IF NOT EXISTS postgis;" \
            -c "CREATE EXTENSION IF NOT EXISTS pg_trgm;" \
            -c "CREATE EXTENSION IF NOT EXISTS unaccent;" \
            -c "CREATE EXTENSION IF NOT EXISTS vector;"
      - name: Migrate + seed
        run: |
          npm run db:test:migrate
          npm run db:test:seed
      - name: Integration tests
        run: npm run test:integration

  # ── 4. Build – depends on static analysis ────────────────────────────────
  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [typecheck, lint, format]
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - name: Build frontend
        run: npm run build
      - name: Upload dist artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist
          if-no-files-found: error

  # ── 5. E2E – depends on build + integration (DB seeded) ──────────────────
  test-e2e:
    name: E2E tests
    runs-on: ubuntu-latest
    needs: [build, test-integration]
    permissions:
      contents: read
    services:
      postgres:
        image: ghcr.io/jbmbab/milj-beslut-postgres-test:16
        env:
          POSTGRES_DB: miljobeslut_test
          POSTGRES_USER: miljobeslut
          POSTGRES_PASSWORD: password
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U miljobeslut -d miljobeslut_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 15
    env:
      NODE_ENV: test
      DATABASE_URL: postgresql://miljobeslut:password@localhost:5432/miljobeslut_test
      JWT_ACCESS_SECRET: ci-access-secret
      JWT_REFRESH_SECRET: ci-refresh-secret
      ADMIN_CONSOLE_USERNAME: admin
      ADMIN_CONSOLE_PASSWORD: admin
      ADMIN_ORG_NAME: Miljobeslut Test Org
      ADMIN_ORG_NUMBER: 999999-0001
      LANTMATERIET_OPEN_MODE: 'true'
      LANTMATERIET_BASE_URL: https://example.invalid
      SLU_API_BASE_URL: https://example.invalid
      SLU_API_KEY: ci-slu-key
      SEARCH_WORKER_ENABLED: 'false'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - run: cp .env.test.example .env.test
      - name: Enable PostGIS + pgvector extensions
        run: |
          PGPASSWORD=password psql -h localhost -U miljobeslut -d miljobeslut_test \
            -c "CREATE EXTENSION IF NOT EXISTS postgis;" \
            -c "CREATE EXTENSION IF NOT EXISTS pg_trgm;" \
            -c "CREATE EXTENSION IF NOT EXISTS unaccent;" \
            -c "CREATE EXTENSION IF NOT EXISTS vector;"
      - name: Migrate + seed
        run: |
          npm run db:test:migrate
          npm run db:test:seed
      - name: Download dist artifact
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist
      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium
      - name: E2E tests
        run: npm run test:e2e
      - name: Upload Playwright artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-artifacts
          path: |
            playwright-report
            test-results
          if-no-files-found: ignore
